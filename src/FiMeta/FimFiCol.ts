// Codegen v2
import { FiMeta } from "../FiContainers/FiMeta";

export class FimFiCol {

  public static ofcTxDesc(): FiMeta {
    let fiMeta = FiMeta.create("ofcTxDesc");

    return fiMeta;
  }

  public static ofcTxPrefix(): FiMeta {
    let fiMeta = FiMeta.create("ofcTxPrefix");

    return fiMeta;
  }

  public static ofcTxEntityName(): FiMeta {
    let fiMeta = FiMeta.create("ofcTxEntityName");

    return fiMeta;
  }

  public static ofcTxFieldName(): FiMeta {
    let fiMeta = FiMeta.create("ofcTxFieldName");

    return fiMeta;
  }

  public static ofcTxFieldType(): FiMeta {
    let fiMeta = FiMeta.create("ofcTxFieldType");

    return fiMeta;
  }

  public static ofcTxHeader(): FiMeta {
    let fiMeta = FiMeta.create("ofcTxHeader");

    return fiMeta;
  }

  public static ofcTxDbField(): FiMeta {
    let fiMeta = FiMeta.create("ofcTxDbField");

    return fiMeta;
  }

  public static ofcTxRefField(): FiMeta {
    let fiMeta = FiMeta.create("ofcTxRefField");

    return fiMeta;
  }

  public static ofcLnLength(): FiMeta {
    let fiMeta = FiMeta.create("ofcLnLength");

    return fiMeta;
  }

  public static ofcLnPrecision(): FiMeta {
    let fiMeta = FiMeta.create("ofcLnPrecision");

    return fiMeta;
  }

  public static ofcLnScale(): FiMeta {
    let fiMeta = FiMeta.create("ofcLnScale");

    return fiMeta;
  }

  public static ofcBoNullable(): FiMeta {
    let fiMeta = FiMeta.create("ofcBoNullable");

    return fiMeta;
  }

  public static ofcTxIdType(): FiMeta {
    let fiMeta = FiMeta.create("ofcTxIdType");

    return fiMeta;
  }

  public static ofcBoTransient(): FiMeta {
    let fiMeta = FiMeta.create("ofcBoTransient");

    return fiMeta;
  }

  public static ofcBoUnique(): FiMeta {
    let fiMeta = FiMeta.create("ofcBoUnique");

    return fiMeta;
  }

  public static ofcBoUniqGro1(): FiMeta {
    let fiMeta = FiMeta.create("ofcBoUniqGro1");

    return fiMeta;
  }

  public static ofcTxDefValue(): FiMeta {
    let fiMeta = FiMeta.create("ofcTxDefValue");

    return fiMeta;
  }


}

// Codegen v2
import { FiMeta } from "../FiContainers/FiMeta";

export class FimFiCol {

  public static fcTxDesc(): FiMeta {
    let fiMeta = FiMeta.create("fcTxDesc");

    return fiMeta;
  }

  public static fcTxPrefix(): FiMeta {
    let fiMeta = FiMeta.create("fcTxPrefix");

    return fiMeta;
  }

  public static fcTxEntityName(): FiMeta {
    let fiMeta = FiMeta.create("fcTxEntityName");

    return fiMeta;
  }

  public static fcTxFieldName(): FiMeta {
    let fiMeta = FiMeta.create("fcTxFieldName");

    return fiMeta;
  }

  public static fcTxFieldType(): FiMeta {
    let fiMeta = FiMeta.create("fcTxFieldType");

    return fiMeta;
  }

  public static fcTxHeader(): FiMeta {
    let fiMeta = FiMeta.create("fcTxHeader");

    return fiMeta;
  }

  public static fcTxDbField(): FiMeta {
    let fiMeta = FiMeta.create("fcTxDbField");

    return fiMeta;
  }

  public static fcTxRefField(): FiMeta {
    let fiMeta = FiMeta.create("fcTxRefField");

    return fiMeta;
  }

  public static fcLnLength(): FiMeta {
    let fiMeta = FiMeta.create("fcLnLength");

    return fiMeta;
  }

  public static fcLnPrecision(): FiMeta {
    let fiMeta = FiMeta.create("fcLnPrecision");

    return fiMeta;
  }

  public static fcLnScale(): FiMeta {
    let fiMeta = FiMeta.create("fcLnScale");

    return fiMeta;
  }

  public static fcBoNullable(): FiMeta {
    let fiMeta = FiMeta.create("fcBoNullable");

    return fiMeta;
  }

  public static fcTxIdType(): FiMeta {
    let fiMeta = FiMeta.create("fcTxIdType");

    return fiMeta;
  }

  public static fcBoTransient(): FiMeta {
    let fiMeta = FiMeta.create("fcBoTransient");

    return fiMeta;
  }

  public static fcBoUnique(): FiMeta {
    let fiMeta = FiMeta.create("fcBoUnique");

    return fiMeta;
  }

  public static fcBoUniqGro1(): FiMeta {
    let fiMeta = FiMeta.create("fcBoUniqGro1");

    return fiMeta;
  }

  public static fcTxDefValue(): FiMeta {
    let fiMeta = FiMeta.create("fcTxDefValue");

    return fiMeta;
  }


}
